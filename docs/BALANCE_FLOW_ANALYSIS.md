# 📊 การวิเคราะห์ Flow การคำนวณยอดยกมา (Opening Balance)

## 1. ข้อมูลที่พบจากการสำรวจ (สินค้า 1010010001 คลัง 42G1)

| แหล่งข้อมูล | ยอด (ลัง) | หมายเหตุ |
|-------------|-----------|---------|
| **Legacy: ตาราง `stocks`** | 55 ลัง | ยอดปัจจุบันจริงใน DB เก่า |
| **Legacy: ผลรวม Transactions** | 160 ลัง | รวมจาก `transaction_details` ที่ whcode='42G1' |
| **Legacy: ใบ BEG (Brought Forward)** | 0 ลัง | ไม่มีใบ BEG ใน Legacy สำหรับสินค้านี้ |
| **ระบบใหม่: ผลรวม Transactions** | 160 ลัง | ตรงกับ Legacy (หลัง Migrate) |
| **Legacy: ยอด BF ที่คุณต้องการ** | 39 ลัง | ตามรายงานสิ้นปี 2024 |

---

## 2. ⚠️ ปัญหาที่พบ: ยอดไม่ตรงกัน

### 2.1 ระบบเก่า (Legacy)
```
ตาราง stocks.balance = 55 ลัง
ผลรวม transactions = 160 ลัง
ความแตกต่าง = 105 ลัง (หายไปไหน?)
```

**สาเหตุที่เป็นไปได้:**
1. มี Transaction ที่ถูก "Delete" หรือ "Cancel" ไปแล้ว แต่ยอดใน Stock ถูกปรับตามจริง
2. มีการ Manual Adjust ยอดในตาราง `stocks` โดยตรง (ไม่ผ่าน Transaction)
3. มีเอกสาร/Transaction บางประเภทที่ไม่ถูกนำมาคำนวณ

### 2.2 ระบบใหม่ (After Migration)
```
ผลรวม transactions = 160 ลัง (Migrate มาจาก Legacy ตรงๆ)
```
เนื่องจากการ Migrate ไม่ได้ Migrate ตาราง `stocks` โดยตรง แต่คำนวณจาก Transactions ใหม่ ยอดจึงกลายเป็น 160 แทนที่จะเป็น 55

---

## 3. 🔍 Flow การคำนวณยอดยกมาในระบบใหม่

### 3.1 Logic ใน `page.tsx` (stock-movement report)

```typescript
// ดึงรายการ Transaction ทั้งหมดก่อนวันที่เลือก
const movements = await prisma.transactionDetail.findMany({
    where: {
        productCode,
        whCode,  // ⭐ กรองที่ระดับ Detail (แก้ไขแล้ว)
        header: {
            docStatus: "APPROVED",
            docDate: { lt: date },
            OR: [{ whCode: whCode }, { toWhCode: whCode }]
        }
    }
});

// คำนวณยอด
let balance = 0;
movements.forEach(m => {
    if (docTypeCode === "TRN") {
        // โอนสินค้า: recordType='1' = ออก, recordType='2' = เข้า
        if (recordType === '1') balance -= qty;
        else if (recordType === '2') balance += qty;
    } else {
        // ประเภทอื่น: ดูจาก movementType
        if (movementType === "IN") balance += qty;
        else balance -= qty;
    }
});
```

### 3.2 ความแตกต่างที่พบ (และแก้ไขแล้ว)

| ก่อนแก้ไข | หลังแก้ไข |
|-----------|-----------|
| ดึง Transaction โดยดูแค่ `header.whCode` หรือ `header.toWhCode` | เพิ่มเงื่อนไข `detail.whCode = whCode` |
| ทำให้ TRN 1 ใบ ถูกนับ 2 รอบ (ทั้ง Record ต้นทาง+ปลายทาง) | นับเฉพาะ Record ที่ `whCode` ตรงกับคลังที่เลือก |

---

## 4. 🔧 สาเหตุหลักของความคลาดเคลื่อน

### 4.1 ระบบเก่าใช้ "ยอดตั้งต้น" ที่ไม่ได้มาจาก Transaction
ระบบเก่ามักมียอดตั้งต้นที่ถูกใส่ตรงๆ ลงในตาราง `stocks` (Go-Live Balance) โดยไม่มี Transaction ประกอบ

**ทางแก้:** สร้างใบ BEG (Beginning Balance) เพื่อปรับยอดให้ตรง

### 4.2 การ Migrate Transaction แต่ไม่ Migrate ยอดตั้งต้น
Script `migrate-full.js` ย้าย Transaction ทั้งหมด แต่ตารางยอด Stock ที่ Migrate มา จะใช้ได้เฉพาะกรณีที่ไม่ต้องการคำนวณย้อนหลัง

**ทางแก้:** ถ้าต้องการแสดงยอดย้อนหลังให้ถูกต้อง ต้อง:
- หายอด Brought Forward (BF) ณ วันเริ่มต้นของระบบเก่า
- สร้างใบ BEG สำหรับสินค้าที่มียอดตั้งต้นไม่ตรง

---

## 5. 📋 สิ่งที่ต้องตรวจสอบก่อน Migrate ใหม่

1. **ยอดตั้งต้นจริงๆ คืออะไร?**
   - ยอดใน `stocks` table = 55 ลัง (ยอดปัจจุบัน)
   - ยอด BF/BEG ณ วันเริ่มต้นระบบ = ?
   
2. **Transaction แรกสุดในระบบเก่าคือวันที่เท่าไหร่?**
   - ถ้าระบบเริ่มใช้งานวันที่ 1 ม.ค. 2023 และยอดตั้งต้น = X ลัง
   - ต้องสร้างใบ BEG วันที่ 31 ธ.ค. 2022 จำนวน X ลัง

3. **ยอดที่คุณต้องการให้ระบบใหม่แสดง ณ 1 ม.ค. 2025 คือ?**
   - 39 ลัง (ตามที่แจ้ง)
   - จะต้องคำนวณย้อนกลับว่าต้องใส่ BEG เท่าไหร่

---

## 6. 🎯 ข้อเสนอแนะ

### Option A: ใช้ยอด Stock ปัจจุบันเป็นหลัก (ไม่สร้าง BEG)
- **ข้อดี:** ง่าย ไม่ต้องแก้อะไร
- **ข้อเสีย:** รายงานย้อนหลังจะไม่ถูกต้อง

### Option B: สร้าง BEG ตามยอดตั้งต้นจริง (ที่หายไป)
สูตร:
```
BEG = ยอดที่ต้องการ - ผลรวม Transactions
BEG = 39 - 160 = -121 ลัง (ต้องลบออก 121 ลัง)
```
หรือถ้านับใหม่หลัง Migrate Clean:
```
BEG = ยอดที่ต้องการ - ผลรวม Transactions ใหม่
```

### Option C: Migrate แบบใช้ Snapshot ยอด Stock
แก้ไข `migrate-full.js` ให้สร้างใบ BEG อัตโนมัติจากยอดใน `stocks` table ของระบบเก่า

---

## 7. 📝 สรุป

| หัวข้อ | สถานะ |
|--------|-------|
| Bug Double Counting ใน Report | ✅ แก้ไขแล้ว |
| ใบ BEG-MANUAL-001 ที่สร้างผิด | ✅ ลบแล้ว |
| ยอด Transaction ระบบใหม่ตรงกับเก่า | ✅ ตรง (160 ลัง) |
| ยอดตั้งต้น (BF) ถูก Migrate หรือยัง | ❌ ยังไม่ได้ Migrate |

**ขั้นตอนถัดไป:** รอคุณ Migrate ใหม่ และตัดสินใจว่าจะจัดการยอดตั้งต้นอย่างไร
