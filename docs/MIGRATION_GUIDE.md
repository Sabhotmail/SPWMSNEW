# 📦 คู่มือการ Migrate ข้อมูลจากระบบ SPWMS เก่าไปยังระบบใหม่

> **เวอร์ชัน:** 1.0  
> **วันที่อัพเดท:** 15 มกราคม 2026  
> **ผู้จัดทำ:** ทีมพัฒนาระบบ SPWMS

---

## 📋 สารบัญ

1. [ภาพรวมการ Migration](#1-ภาพรวมการ-migration)
2. [ข้อกำหนดเบื้องต้น](#2-ข้อกำหนดเบื้องต้น)
3. [ขั้นตอนการ Migration](#3-ขั้นตอนการ-migration)
4. [การตรวจสอบหลัง Migration](#4-การตรวจสอบหลัง-migration)
5. [การแก้ไขปัญหาที่พบบ่อย](#5-การแก้ไขปัญหาที่พบบ่อย)

---

## 1. ภาพรวมการ Migration

### 🎯 วัตถุประสงค์
ย้ายข้อมูลจากระบบ SPWMS เดิม (PostgreSQL บน 192.168.10.15) ไปยังระบบใหม่ (Next.js + Prisma)

### 📊 ข้อมูลที่ย้าย

| ลำดับ | ประเภทข้อมูล | คำอธิบาย |
|-------|--------------|----------|
| 1 | Master Data | สาขา, ผู้จัดจำหน่าย, แบรนด์, หน่วยนับ |
| 2 | Users | ข้อมูลผู้ใช้งานทั้งหมด |
| 3 | Warehouses | คลังสินค้าและ Location |
| 4 | Products | สินค้าและหน่วยนับของสินค้า |
| 5 | Document Config | ประเภทเอกสาร, ประเภทการเคลื่อนไหว |
| 6 | Stock Balance | ยอดคงเหลือสินค้าปัจจุบัน |
| 7 | Stock Dates | ข้อมูล Lot และวันหมดอายุ |
| 8 | Transactions | เอกสารรับ-จ่าย ที่อนุมัติแล้ว |

### ⏱️ ระยะเวลาโดยประมาณ
- **Migration ทั้งหมด:** ประมาณ 5-10 นาที
- **ตรวจสอบความถูกต้อง:** ประมาณ 30 นาที

---

## 2. ข้อกำหนดเบื้องต้น

### 🖥️ ระบบที่ต้องเตรียม

```
✅ Node.js v18 ขึ้นไป
✅ PostgreSQL (สำหรับ Database ใหม่)
✅ เข้าถึง Database เก่าได้ (192.168.10.15)
✅ Network เชื่อมต่อระหว่าง Server เก่า-ใหม่
```

### 📁 ไฟล์สคริปต์ที่ใช้

```
📂 spwms-new/
├── migrate-full.js        # ย้าย Master Data + Stock
├── migrate-documents.js   # ย้ายเอกสาร Transaction
└── .env                   # ตั้งค่า Database ใหม่
```

### ⚙️ การตั้งค่า .env

```env
# Database ใหม่
DATABASE_URL="postgresql://postgres:PASSWORD@localhost:5432/spwms_new?schema=public"

# NextAuth
AUTH_SECRET="your-secret-key"
NEXTAUTH_URL="http://localhost:3000"
```

---

## 3. ขั้นตอนการ Migration

### 📌 ขั้นตอนที่ 1: เตรียม Database ใหม่

```bash
# 1.1 สร้าง Database ใหม่
createdb spwms_new

# 1.2 รัน Prisma Migration เพื่อสร้างตาราง
npx prisma migrate deploy

# 1.3 Generate Prisma Client
npx prisma generate
```

### 📌 ขั้นตอนที่ 2: ย้าย Master Data และ Stock

```bash
# รันสคริปต์ย้ายข้อมูลหลัก
node migrate-full.js
```

**ผลลัพธ์ที่คาดหวัง:**
```
🚀 เริ่มกระบวนการ Migration ข้อมูลแบบสมบูรณ์...
📦 1/13 ย้ายข้อมูล Branches...
   ✅ 2 รายการ
👤 2/13 ย้ายข้อมูล Users...
   ✅ 14 รายการ
...
📊 12/13 ย้ายยอด Stock ปัจจุบัน...
   ✅ 212 รายการ
📅 13/13 ย้ายยอด StockDate (Lot ข้อมูล)...
   ✅ 662 รายการ

==================================================
✅ Migration สำเร็จทั้งหมด!
⏱️ ใช้เวลาทั้งสิ้น: 4.95 วินาที
==================================================
```

### 📌 ขั้นตอนที่ 3: ย้ายเอกสาร Transaction

```bash
# รันสคริปต์ย้ายเอกสาร (เฉพาะที่ ACTIVE/APPROVED)
node migrate-documents.js
```

**ผลลัพธ์ที่คาดหวัง:**
```
🚀 เริ่มกระบวนการ Migration เอกสาร...
📄 กำลังดึงข้อมูลเอกสารจากระบบเก่า...
   พบเอกสาร: 8985 รายการ

   📝 ประมวลผล: 100/8985 (สำเร็จ: 100, ข้าม: 0, ผิดพลาด: 0)
   ...

🔢 กำลังอัพเดท Running Number...
   IN: ปี 2026 -> เลขล่าสุด 123
   OUT: ปี 2026 -> เลขล่าสุด 456
   ...

==================================================
✅ Migration เอกสารสำเร็จ!
==================================================
```

### 📌 ขั้นตอนที่ 4: เริ่มต้นระบบใหม่

```bash
# Build และ Start Application
npm run build
npm run start

# หรือใช้ PM2 สำหรับ Production
pm2 start npm --name "spwms" -- start
```

---

## 4. การตรวจสอบหลัง Migration

### ✅ Checklist การตรวจสอบ

| # | รายการตรวจสอบ | วิธีตรวจสอบ |
|---|---------------|-------------|
| 1 | Login ได้ด้วย User เดิม | ทดสอบ Login หน้าเว็บ |
| 2 | ยอดสต็อกถูกต้อง | เปรียบเทียบรายงานยอดคงเหลือกับระบบเก่า |
| 3 | ดูประวัติเอกสารย้อนหลังได้ | เปิดดูเอกสารเก่าในระบบใหม่ |
| 4 | สร้างเอกสารใหม่ได้ | ทดลองสร้างบิลรับเข้า/จ่ายออก |
| 5 | เลขที่เอกสารต่อเนื่อง | ตรวจสอบเลขที่บิลใหม่ไม่ซ้ำกับเดิม |
| 6 | ข้อมูล Lot/วันหมดอายุถูกต้อง | ตรวจสอบรายงาน Stock Card |

### 🔍 คำสั่งตรวจสอบ Database

```sql
-- ตรวจสอบจำนวนข้อมูลในระบบใหม่
SELECT 'products' as table_name, COUNT(*) FROM products
UNION SELECT 'stocks', COUNT(*) FROM stocks
UNION SELECT 'stock_dates', COUNT(*) FROM stock_dates
UNION SELECT 'transaction_headers', COUNT(*) FROM transaction_headers;
```

### 📊 เปรียบเทียบยอดสต็อก

```sql
-- ระบบใหม่
SELECT product_code, wh_code, SUM(balance) as total
FROM stocks
GROUP BY product_code, wh_code
ORDER BY product_code;

-- เปรียบเทียบกับระบบเก่า (รันที่ 192.168.10.15)
SELECT productcode, whcode, SUM(balance) as total
FROM stocks
GROUP BY productcode, whcode
ORDER BY productcode;
```

---

## 5. การแก้ไขปัญหาที่พบบ่อย

### ❌ ปัญหา: เชื่อมต่อ Database เก่าไม่ได้

**อาการ:** `Error: connect ETIMEDOUT 192.168.10.15:5432`

**วิธีแก้:**
1. ตรวจสอบว่า Network เชื่อมต่อได้: `ping 192.168.10.15`
2. ตรวจสอบว่า PostgreSQL เปิดรับ Connection จากภายนอก
3. ตรวจสอบ Firewall

---

### ❌ ปัญหา: Foreign Key Constraint Error

**อาการ:** `Foreign key constraint failed on the field: ...`

**วิธีแก้:**
1. รัน `migrate-full.js` ก่อน `migrate-documents.js` เสมอ
2. Master Data ต้องถูกย้ายก่อน Transaction

---

### ❌ ปัญหา: Duplicate Key Error

**อาการ:** `Unique constraint failed on the fields: ...`

**วิธีแก้:**
1. สคริปต์ใช้ `upsert` จึงรันซ้ำได้โดยไม่เกิดปัญหา
2. ถ้ายังเกิด ให้ลบข้อมูลเก่าออกก่อน หรือ reset Database

---

### ❌ ปัญหา: ยอดสต็อกไม่ตรง

**สาเหตุที่เป็นไปได้:**
1. มีการทำรายการในระบบเก่าหลังจาก Migration
2. มีเอกสาร DRAFT ค้างในระบบเก่าที่ยังไม่ได้อนุมัติ

**วิธีแก้:**
1. หยุดใช้งานระบบเก่าก่อน Migration
2. รัน Migration ใหม่อีกครั้ง

---

## 📞 ติดต่อสอบถาม

หากพบปัญหาในการ Migration กรุณาติดต่อทีมพัฒนา พร้อมแนบ:
- Screenshot ของ Error Message
- Log จากการรันสคริปต์
- ขั้นตอนที่ทำก่อนเกิดปัญหา

---

## 📝 ประวัติการแก้ไข

| วันที่ | เวอร์ชัน | รายละเอียด |
|--------|----------|-------------|
| 15/01/2026 | 1.0 | สร้างเอกสารครั้งแรก |

