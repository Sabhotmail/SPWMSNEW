services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: spwms-db
    restart: unless-stopped
    environment:
      # Hardcoding for initialization to ensure credentials match
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: S1r1Pr0
      POSTGRES_DB: spwms_production
    volumes:
      - spwms_db_v6_final:/var/lib/postgresql/data
    networks:
      - spwms-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # SPWMS Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spwms-app
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL}
      AUTH_SECRET: ${AUTH_SECRET}
      NEXTAUTH_URL: ${NEXTAUTH_URL}
      AUTH_TRUST_HOST: "true"
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    networks:
      - spwms-network
      - coolify
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.spwms.rule=Host(`192.168.10.157.sslip.io`) || Host(`rgok4soogwcg8o4sgkk0gosg.110.171.206.154.sslip.io`)"
      - "traefik.http.routers.spwms.entrypoints=http"
      - "traefik.http.services.spwms.loadbalancer.server.port=3000"

networks:
  spwms-network:
    driver: bridge
  coolify:
    external: true

volumes:
  spwms_db_v6_final:
    driver: local
