# 🐳 Docker Deployment Guide

## ใช้งาน Docker (Start Fresh):

### สถานะปัจจุบัน:
✅ Docker กำลังรันอยู่
✅ Database: spwms_production (ว่าง - เริ่มต้นใหม่)
✅ Application: http://localhost:3000

### ข้อมูลเริ่มต้น:
- Users: admin (admin/admin123)
- Warehouses: WH01, WH02, 42G1
- Branches: HQ, 001, 002
- Products: ว่าง (เริ่มบันทึกใหม่)
- Transactions: ว่าง (เริ่มบันทึกใหม่)

### การใช้งาน:

1. **Login**
   ```
   URL: http://localhost:3000/login
   User: admin
   Password: admin123
   ```

2. **เริ่มบันทึกข้อมูล**
   - เพิ่มสินค้า → Master → Products
   - บันทึกรับสินค้า → Transactions → Receive
   - บันทึกจ่ายสินค้า → Transactions → Issue
   - ดูรายงาน → Reports

3. **จัดการ Docker**
   ```powershell
   # ดู logs
   docker-compose logs -f app
   
   # Restart
   docker-compose restart app
   
   # Stop
   docker-compose down
   
   # Start อีกครั้ง
   docker-compose --env-file .env.docker up -d
   ```

### ข้อดี:
- ทดสอบ production deployment ได้
- ข้อมูลแยกจาก development
- พร้อม deploy จริงได้เลย

### ข้อจำกัด:
- ไม่มีข้อมูล legacy
- ต้องบันทึกข้อมูลใหม่ทั้งหมด
